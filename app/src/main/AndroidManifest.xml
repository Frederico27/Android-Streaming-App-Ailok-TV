<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.smallcademy.livetvapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_foreground"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.LiveTVapp"
        android:usesCleartextTraffic="true">

        <activity android:name=".CategoryDetails"></activity>
        <activity android:name=".CategoryDetailsHome"></activity>

        <activity android:name=".Youtube_Web"
            android:theme="@style/Theme.LiveTVapp.NoActionBar"
            android:configChanges="orientation|screenSize"/>

        <activity android:name=".CategoryYT"></activity>

        <activity android:name=".Feedback"></activity>

        <activity android:name=".Categories" />

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.LiveTVapp.NoActionBar"></activity>

        <activity
            android:name=".Jaringan_Araska"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.LiveTVapp.NoActionBar"></activity>
        <activity
            android:name=".Details"
            android:configChanges="keyboardHidden|orientation|screenSize" />

        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.LiveTVapp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>